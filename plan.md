1. **Create the Consolidated Backlog File**
   - Use the `run_in_bash_session` tool to execute a command that will output the consolidated backlog document into a new file `docs/consolidated_outcome_backlog.md`.
   - The contents of this file will be generated by combining and summarising the various issue tracking files found in the workspace (`docs/consolidated_product_backlog.md`, `docs/consolidated_issues_backlog.md`, `wiki/Issues_Backlog.md`, `github-issues-report.md`, and `github-issue-communities.md`).
   - The document will be organised around complete user outcomes rather than individual technical chores, resolving duplicates, superseding overly granular items, and removing completed tasks (e.g., the NestJS CRON job for streaks).

2. **Verify File Creation**
   - Use `run_in_bash_session` to run `cat docs/consolidated_outcome_backlog.md` and verify that the file was created successfully and contains the expected content.

3. **Pre-commit Steps**
   - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done. (Although this is a markdown-only change, we must run the verification scripts as required).

4. **Submit Changes**
   - Submit the new file via the `submit` tool with a descriptive commit message like "docs: add consolidated outcome-oriented product backlog".
