The deterministic quality gate found blocking implementation evidence. Fix the underlying production implementation. Do not bypass the gate by merely renaming the mock, deleting the test, skipping the test, disabling linting, or weakening types.

Inspect the full issue and diff, correct the actual vertical slice, run applicable verification, and leave the worktree in a state that genuinely satisfies the issue.
