# Component migration status

The canonical component-system PR owns all remaining `frontend/src/app/components` and `components/primitives` convergence work.

The branch must not merge until its component-system verification, design-sync checks, frontend checks and Spartan health checks are green. CI failures are treated as the remaining migration queue and repaired on this same branch.
