# Canonical component convergence PR scope

This branch is the single integration branch for the remaining Relay + Spartan migration of `frontend/src/app/components` and `frontend/src/app/components/primitives`.

All component/primitive migration work should land here until the shared interaction layer is converged and canonical CI is green. Do not create parallel replacement PRs for the same component-system scope.
