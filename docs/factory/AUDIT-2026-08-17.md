# Factory deep audit: 2026-08-17

## Verdict

The Factory is not working flawlessly and the inspected host is not autonomously processing work.

The repository contains a credible provider-neutral Factory implementation. This revision also restores the
production policy to direct subscription-first routing after a later OpenHands-only configuration regression.
Those source properties do not establish production operation. At 09:14 UTC on 17 August 2026, the canonical
daemon was failed, its health timer was inactive, a retired competing service was restart-looping, the installed
systemd unit was older than the repository unit, service-user provider authentication was unproven, and GitHub
allowed merges without either required Factory status.

No responsible engineering audit can describe that state as 100 per cent autonomous. Activation requires a
root-owned deployment, fail-closed GitHub rules, service-account authentication, and one deliberately small
end-to-end canary. None of those state-changing or subscription-consuming actions was performed during this
read-only production investigation.

This audit is the current readiness verdict. The larger implementation history and control-by-control review are
retained in [AUDIT-2026-08-16.md](AUDIT-2026-08-16.md).

## Post-audit operational update: 10:57 UTC

A later read-only host check found that the three installed Factory service and timer unit files now match their
repository counterparts, and `hellotalk-meta-agent.service` is no longer installed. Those are improvements over
the 09:14 inspection. They do not change the readiness verdict:

- `hellotalk-factory.service` remains failed with `Result=start-limit-hit`;
- `hellotalk-factory-health.timer` remains inactive;
- the `main` ruleset still has broad always-bypass actors and does not require either canonical Factory status;
- dedicated service-user provider authentication and an end-to-end canary remain unproven.

This revision adds the bounded GitHub status and control panel described in
[CONTROL-PANEL.md](CONTROL-PANEL.md). It is source-complete but cannot publish or accept controls until this
revision is deployed from `main`, the timer is active, and the first `dashboard sync` succeeds. A green source
test therefore must not be reported as a live control panel.

## Post-audit storage update: 12:13 UTC

The host root filesystem later reached 100 per cent utilisation. The confirmed causes and recovery evidence are
documented in [HOST-STORAGE.md](HOST-STORAGE.md). Native cache cleanup recovered about 2.6 GiB. The complete
developer OpenCode database and an inactive developer-owned Factory tree were copied and verified on the
secondary volume before their root copies were contracted. Root then had 3.6 GiB available, while the secondary
volume retained about 30 GiB available.

Persistent journals still occupied 3.6 GiB because applying a global retention policy requires root. This
revision adds a bounded journal policy, explicit safe Docker-cache maintenance, and root plus Factory-volume
telemetry in the GitHub panel. Those source changes do not vacuum the live journal until the root deployment or
maintenance command is run.

## Evidence boundary

## Post-audit crash investigation: 17:27 UTC

The operator started the deployed service, but it entered a restart loop. `providers check` passed and
OpenHands was the only usable configured provider. The daemon then failed its fail-closed
`agent-process-isolation` and `verification-isolation` checks and exited with status 1.

The cause was reproduced with the installed systemd restrictions: `ProtectKernelLogs=true` caused both
the provider and verification `unshare --mount-proc` sandboxes to fail with `Operation not permitted`.
The main daemon unit now omits that one incompatible directive while retaining the remaining process,
home, filesystem, cgroup, and network boundaries. The root watchdog retains its kernel-log protection.
The unit change is not live until the `main` deployment is completed and the service reports a fresh
heartbeat.

The same live diagnostics also showed that Claude, Codex, and OpenCode are not installed on the service
user's PATH, and the OpenAI credential is expired. Those are provider readiness warnings, not the cause
of this crash because OpenHands remains configured as an emergency provider.

The source review started from `main` at `8abd960edfdee89a5a43832fa649ed13ff1afb7b` and covered:

- issue and pull-request discovery, queue selection, durable jobs, retries, quarantine, and restart recovery;
- worktree, branch, commit, push, verification, review, status, CI repair, and merge ownership;
- every AI-backed phase and every provider adapter;
- provider process isolation, environment filtering, timeout, cancellation, and shutdown;
- health, circuit, cooldown, capacity, fallback, diversity, provenance, and metrics;
- production configuration, systemd, deployment, doctor, workflows, and active documentation;
- current vendor model guidance and the account-visible developer-user CLI catalogues;
- the live host and GitHub control plane using read-only commands.

Local developer authentication proves only that the installed CLIs can see the configured model names for that
developer account. It does not prove that `hellotalk-factory`, whose home is
`/var/lib/hellotalk-factory/home`, is authenticated or has the same entitlements. Root-protected production
configuration and durable state were not readable from this developer session. No paid model task was launched.

## Readiness matrix

| Area                                 | Audit result               | Evidence                                                                                                                                                                           |
| ------------------------------------ | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Provider-neutral source architecture | Pass                       | One `AgentRouter` owns Claude, Codex, Google, OpenCode, and OpenHands adapters.                                                                                                    |
| Phase-specific production policy     | Corrected in this revision | Claude, Codex, and OpenCode are enabled; Google remains rollout-gated; OpenHands is emergency-only.                                                                                |
| Typed fallback and finite retry      | Pass in source and tests   | Provider-side failures rotate; task, test, repository, policy, and internal failures do not blindly rotate.                                                                        |
| Cross-provider review                | Pass in source and tests   | Every provider that may have mutated code is excluded where alternatives exist; same-provider review is a recorded last resort.                                                    |
| Structured code review               | Pass in source and tests   | `.factory-review.json` is cleared, regenerated, validated, and bound to the current head SHA.                                                                                      |
| Live daemon                          | Fail                       | `hellotalk-factory.service` was `failed` with `Result=start-limit-hit`.                                                                                                            |
| Health recovery timer                | Fail                       | `hellotalk-factory-health.timer` was enabled but `inactive/dead`.                                                                                                                  |
| Single execution owner               | Fail                       | `hellotalk-meta-agent.service` was enabled and auto-restarting with 13,223 restarts; developer-owned Claude, Codex, and Antigravity processes also existed outside Factory leases. |
| Installed runtime parity             | Fail                       | The installed unit hard-coded `user@988.service` and lacked the source unit's runtime environment, provider preflight, provider path, and stronger proc/home isolation.            |
| Service-user provider authentication | Unproven                   | Developer-user auth and catalogues cannot attest the dedicated production account.                                                                                                 |
| GitHub merge enforcement             | Fail                       | The active `main` ruleset required a PR but required zero approvals and zero status checks, with broad always-bypass actors.                                                       |
| End-to-end autonomous canary         | Not run                    | A paid, repository-mutating canary requires the deployment and policy blockers to be cleared first.                                                                                |
| Complete fair backlog processing     | Incomplete                 | 3,657 issues were open and 1,392 carried `factory-active`; discovery still uses a fixed 10,000-item list and selection is priority then oldest numeric identifier.                 |

Recent green GitHub workflow runs prove that those revisions passed their workflows. They do not prove that the
failed VPS daemon discovered, implemented, reviewed, or merged the work.

## Critical findings

### P0: the production daemon is down

`hellotalk-factory.service` cannot discover or advance durable work while it remains failed and start-limited.
The inactive health timer is not recovering it. This alone explains why the canonical Factory has not worked on
the queue.

The repository contains a safer unit and deployment path, but that source has not replaced the installed unit.
The source service runs `providers check` before generation takeover, loads `runtime.env`, exposes the declared
provider executable paths, avoids a host-specific UID dependency, and strengthens home and proc isolation.

### P0: GitHub can merge around the Factory

Ruleset `20604751` is active on `main`, but it does not require `CI / required` or
`factory/independent-review`. Its pull-request rule requires zero approvals, and its broad bypass list includes
repository roles, integrations, and a deploy key. The scheduled merge workflow is a useful final checker, but it
cannot compensate for a server policy that permits another actor to bypass it.

The required contexts must be enforced by one no-bypass ruleset and pinned to expected GitHub App publishers.
Context names alone do not attest who published a legacy commit status.

### P0: more than one execution owner remains active

The retired `hellotalk-meta-agent.service` referenced removed code and had reached 13,223 restarts at inspection.
Long-running developer-owned Claude, Codex, and Antigravity processes were also outside Factory worktrees,
capacity leases, provider history, verification, and merge ownership. They may be legitimate interactive work,
but they cannot be counted as Factory activity and must not share autonomous production ownership.

The repository deploy script disables and removes the retired service. Production is not single-owner until that
script is deployed and `hellotalk-factory legacy scan` passes.

### P1: production routing had regressed to OpenHands-only

The provider-neutral implementation entered history in `36448822f`, but later commits beginning with
`21ae8717b` disabled every direct provider and reduced every production route to `openhands`. Follow-up tests and
documents then encoded the regression as intended behaviour. That configuration could never satisfy the requested
Claude, Codex, Google, OpenCode, then OpenHands fallback policy.

This revision restores direct Claude, Codex, and OpenCode subscription adapters, keeps Google present but disabled
until its service-user canary, places OpenHands last and emergency-only, and adds regression assertions for the
exact phase routes. OpenHands remains one provider beneath the Factory rather than a competing control plane.

### P1: deployment deliberately preserves operator routing

`scripts/deploy-and-start-factory.sh` refreshes `agents.example.json` but preserves an existing
`/etc/hellotalk-factory/agents.json`. That is the correct safe default, but it means deploying this source does not
automatically correct an older OpenHands-only operator file. The operator must compare and deliberately reconcile
the two files before starting the canary.

### P1: service-account provider capacity is unknown

The no-generation developer checks found:

- Claude Code 2.1.233, authenticated through `claude.ai` with a Pro subscription;
- Codex CLI 0.147.0, authenticated through ChatGPT;
- Antigravity 1.1.13 with `gemini-3.1-pro-high` in its model catalogue;
- OpenCode 1.18.15 with OpenCode Go authentication and all configured model slugs.

These are useful adapter-contract checks, not production health. Each check must succeed as
`hellotalk-factory` with the source unit's exact `HOME` and `PATH` before enabling that provider.

## Source architecture review

The current source preserves the important Factory guarantees:

- `FactoryPipeline` owns the lifecycle and maps all AI-backed phases to typed `AgentPhase` values;
- provider implementation details remain in independent adapters rather than phase branches;
- candidate order comes from validated configuration and emergency providers are demoted;
- disabled, unhealthy, cooling-down, unsupported, excluded, and optionally busy providers are skipped;
- provider retries and the candidate set are finite;
- provider-side failures update durable health and can fall through to another provider;
- repository, test, task, policy, and internal failures return to Factory repair or failure handling;
- capacity leases are generation-aware and released in `finally` paths;
- no-provider capacity defers a job without consuming a task attempt;
- health, cooldowns, history, task state, and reviewed SHA survive daemon restart;
- direct CLIs use argument vectors, stdin, bounded output, process groups, TERM then KILL, and no shell command
  constructed from issue text;
- API keys, GitHub tokens, alerting secrets, proxy variables, other provider sessions, and unrelated host paths are
  excluded from direct subscription-provider environments;
- verification runs separately without provider credentials or network access;
- stale review artefacts are deleted before every attempt and valid file output remains authoritative;
- every repair returns to local verification and independent review;
- merge eligibility requires literal success for both canonical statuses and an unchanged head SHA;
- the merge call uses `--match-head-commit` and never uses administrator bypass.

This is a strong resilient design. The failures found in this audit do not justify replacing the Factory with a
single CLI. They justify deploying and enforcing the Factory that already exists.

## August 2026 model policy

There is no timeless universal best model. The following is the quality-first policy supported by current primary
vendor guidance and the model catalogues visible during this audit:

| Phase                     | Primary                        | Ordered fallback                                                                             |
| ------------------------- | ------------------------------ | -------------------------------------------------------------------------------------------- |
| Planning and architecture | Claude Fable 5, maximum effort | GPT-5.6 Sol maximum effort, Gemini 3.1 Pro High, OpenCode Qwen3.8 Max, OpenHands GPT-5.6 Sol |
| Implementation            | Claude Fable 5, maximum effort | GPT-5.6 Sol maximum effort, Gemini 3.1 Pro High, OpenCode Kimi K3, OpenHands GPT-5.6 Sol     |
| Security review           | Claude Fable 5, maximum effort | GPT-5.6 Sol maximum effort, Gemini 3.1 Pro High, OpenCode Qwen3.8 Max, OpenHands GPT-5.6 Sol |
| Code review               | GPT-5.6 Sol, maximum effort    | Claude Fable 5, Gemini 3.1 Pro High, OpenCode Qwen3.8 Max, OpenHands GPT-5.6 Sol             |
| Quality and CI repair     | GPT-5.6 Sol, maximum effort    | Claude Fable 5, Gemini 3.1 Pro High, OpenCode Kimi K3, OpenHands GPT-5.6 Sol                 |
| General mechanical action | OpenCode Kimi K2.7 Code        | Gemini 3.1 Pro High, GPT-5.6 Sol, Claude Fable 5, OpenHands GPT-5.6 Sol                      |

Google remains disabled in the production-safe starting file. It appears in route order so enabling it after the
service-user canary requires only a configuration change. OpenCode choices are a reasoned catalogue-based policy,
not a repository benchmark result. The Factory should collect completion, review, duration, repair, and quota
evidence before promoting a different model.

Detailed commands, references, exact slugs, and trust boundaries are in
[AGENT-ROUTING.md](AGENT-ROUTING.md) and [SUBSCRIPTION-AGENTS.md](SUBSCRIPTION-AGENTS.md).

## Known source gaps

The following items prevent a claim of flawless autonomy even after deployment:

1. [Issue 7026](https://github.com/elgansayer/elgl/issues/7026): discovery uses a fixed 10,000-item request and
   queue selection can starve newer same-priority work behind a very large old backlog.
2. [Issue 6938](https://github.com/elgansayer/elgl/issues/6938): timestamps and state alone do not provide a rich
   progress signal or proactive stagnation recovery.
3. [Issue 6943](https://github.com/elgansayer/elgl/issues/6943): source deployment is safe, but atomic
   last-known-good self-update and rollback remain unfinished.
4. [Issue 6946](https://github.com/elgansayer/elgl/issues/6946): the health timer is host-local; an independent
   external dead-man controller remains unfinished.
5. [Issue 6945](https://github.com/elgansayer/elgl/issues/6945): ambiguous or partially recovered work needs a
   stronger never-discard evidence path.
6. [Issue 6936](https://github.com/elgansayer/elgl/issues/6936): pull-request churn and current-main replay loops
   need stronger bounds.
7. Security review does not yet require an authoritative structured report. A successful provider no-op or
   refusal can be accepted as no findings before deterministic verification runs.
8. CI repair receives failed check names and mergeability, not a bounded redacted log excerpt or base-branch
   attribution.
9. Google and OpenCode lack the equally strong provider-native system-instruction channels used by Claude,
   Codex, and OpenHands in the verified non-interactive contracts.
10. Required status names are checked, but publisher identity is not attested without a dedicated GitHub App and
    ruleset integration pinning.
11. PAYG budget fields are hooks and observability, not exact enforceable cross-vendor billing caps.
12. Cross-repository pull requests are intentionally excluded from mutation because the token cannot safely push
    a fork branch.
13. OpenHands records outer `openhands-sdk` provenance while its inner model attribution remains in a separate
    file.
14. Adaptive provider scoring is not implemented. Current model selection is explicit policy, not automatic
    empirical promotion.

## Required activation sequence

Complete these steps in order:

1. Change the `main` ruleset to require pull requests, `CI / required`, and
   `factory/independent-review`, remove broad bypass actors, and pin expected status publishers.
2. Stop unmanaged autonomous agents, deploy current clean `origin/main`, and confirm the retired meta-agent is
   disabled and removed.
3. Compare `/etc/hellotalk-factory/agents.json` with `agents.example.json` and explicitly adopt the reviewed
   subscription-first policy.
4. Install and authenticate Claude, Codex, and OpenCode as `hellotalk-factory`. Keep Google disabled until its
   own non-interactive service-user canary succeeds.
5. Run `hellotalk-factory providers check`, `hellotalk-factory doctor --online`, `hellotalk-factory legacy scan`,
   systemd status, and the rootless worker diagnostics.
6. Start the service and timer, then prove the heartbeat and durable queue advance without restart churn.
7. Run one small trusted issue through implementation, security, verification, independent cross-provider review,
   CI, merge, and issue closure.
8. Inspect bounded logs and persisted provenance for the canary. Confirm no secret, prompt transcript, stale
   status, same-provider shortcut, orphan process, or attempt-burning no-provider loop occurred.

Do not bulk-enable 1,392 active issues before that canary passes.

## Further recommendations

After activation blockers, prioritise the open Factory issues above, then:

1. Make security review file-authoritative with a validated `.factory-security.json`, stale-file deletion, one
   corrective retry, and provider fallback.
2. Give CI repair a bounded, redacted failed-log excerpt and identify whether the same failure already exists on
   `main`.
3. Add repository-specific provider evaluations for implementation, security, review, and CI repair. Use the
   existing metrics to promote models only when quality gates support the change.
4. Provision a dedicated Factory GitHub App for statuses and merge operations, with minimum permissions and
   ruleset source pinning.
5. Add a vendor-domain egress proxy or short-lived credential broker for direct provider subprocesses.
6. Add a no-cost synthetic router exercise to the health timer so circuit, capacity, structured output, and
   fallback behaviour are checked continuously without consuming subscription quota.
7. Add a read-only review path for fork pull requests rather than silently treating them as repairable.
8. Review provider CLI versions, account-visible model slugs, credential paths, and runtime paths monthly and
   after every CLI update. Fail closed on removal rather than silently substituting a weaker model.

## Definition of safe autonomy

Autonomy means the Factory can discover eligible work, recover durable state, route around provider-side
failures, wait through provider exhaustion, verify changes, obtain independent review, repair attributable
failures, and merge only the reviewed head without routine operator intervention.

It does not mean bypassing authentication renewal, ambiguous product decisions, repository policy failures,
unavailable GitHub infrastructure, security findings, or every-provider exhaustion. In those cases, safe autonomy
keeps the daemon and durable queue recoverable while stopping unsafe task or merge progress. Claiming 100 per cent
operation requires measured production evidence, not only source code and mocked tests.

## Validation status

Local validation completed successfully on 17 August 2026:

- Factory Ruff format and lint passed;
- Factory mypy passed across 55 source files;
- all 476 Factory tests completed, with 472 passing and the four explicitly gated live-provider smokes skipped;
- the root `npm run verify` gate passed every constitution, agent-governance, design, RTL, translation, dependency,
  lint, build, test, conflict-marker, and backend diagnostic stage;
- frontend tests passed 1,823 cases with 473 configured skips;
- backend tests passed 2,856 of 2,856 cases;
- admin tests passed 10 of 10 cases;
- frontend, backend, and admin production builds passed;
- the live backend diagnostic passed 9 of 9 checks;
- `git diff --check` passed and no changed line introduced an em dash.

The existing frontend and backend lint debt remained warning-only and this Factory change introduced none of it.
Normal CI did not use provider credentials and no paid implementation smoke was run. Post-push GitHub CI remains
separate evidence from this local source gate and does not replace the production activation canary.
