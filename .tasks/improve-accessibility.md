* Priority: "High Impact"
* Description: Ensure the application meets WCAG accessibility standards by improving colour contrast, adding descriptive ARIA labels, and ensuring adequate touch target sizes for mobile users.
* Technical Implementation: Audit existing SCSS variables and Tailwind utility classes to ensure all text meets the WCAG 2.1 AA contrast ratio (4.5:1 for normal text). Add appropriate `aria-label`, `aria-expanded`, and `role` attributes to all interactive components (buttons, toggles, modals). Ensure all interactive elements have a minimum size of 44x44 CSS pixels by updating padding or using specific Tailwind sizing classes (e.g., `min-h-[44px] min-w-[44px]`), particularly in the chat navigation and discovery filter bars. Implement robust keyboard navigation logic within custom dropdowns and modals using `HostListener` to trap focus and handle Tab/Escape key events.
