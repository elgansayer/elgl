* Priority: "High Impact"
* Description: Redesign the chat and community interface to emulate the dense but highly readable style of Discord/X. This includes clear server/group navigation, unread message indicators, and distinct active states for channels and users.
* Technical Implementation: Update `chat-page.component.ts` and its HTML to use a dual-pane layout on desktop (CSS Grid or Flexbox) and a collapsible side drawer for mobile (using Angular CDK Overlay or a custom animated drawer). Implement dense typography scales (e.g., Tailwind's `text-sm`, `leading-tight`) for messages to maximize screen real estate. Add visual indicators for unread messages and active states using prominent brand colours (e.g., `bg-accent-500` dot indicators). Use Angular Signals to efficiently manage and reflect the active state of channels and real-time online status of users.
