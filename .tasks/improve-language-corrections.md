* Priority: "High Impact"
* Description: Implement HelloTalk/HiNative style features for language correction and feedback. This includes inline text corrections (diff view), side-by-side or easily accessible translation views, and prominent, easy-to-use audio recording buttons for pronunciation practice.
* Technical Implementation: Create new standalone Angular components (e.g., `inline-correction.component.ts`) to handle the diff logic (comparing original text to corrected text and highlighting additions/deletions with green/red backgrounds). Integrate translation functionality into the message context menu, utilizing a slide-down animation or a small tooltip modal to display the translated text without navigating away. For the audio recording, implement a push-to-talk button using the Web Audio API, capturing the stream and visualizing it using a simple CSS animation or canvas before uploading. Ensure the audio button has a large touch target and clear state indicators (idle, recording, sending).
