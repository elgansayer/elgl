* Priority: Medium Impact
* Description: Address friction points related to mobile responsiveness, touch targets, and overall accessibility. Contrast ratios (e.g., amber text on transparent backgrounds) need reviewing. Add micro-interactions for sending messages, liking moments, and switching tabs to provide immediate tactile feedback.
* Technical Implementation: Enforce a minimum touch target size of 44x44px for all interactive elements in global SCSS or Tailwind config. Audit and update ARIA labels, `aria-expanded`, and keyboard navigation support on custom components (like the custom mention listbox in the chat room). Introduce CSS transition states (`hover`, `active`, `focus-visible`) and use `@angular/animations` for subtle scale and fade effects on buttons and list items.
