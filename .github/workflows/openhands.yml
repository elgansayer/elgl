name: AI Auto-Resolver

on:
  issue_comment:
    types: [created]

concurrency:
  # Per-issue concurrency: different issues run in parallel, same issue is serialised.
  group: openhands-issue-${{ github.event.issue.number || github.run_id }}
  cancel-in-progress: false

jobs:
  ai-agent:
    # This workflow is an issue-to-PR resolver. PR conversations and review comments
    # are handled by the dedicated PR reviewer so we never resolve them from main or
    # accidentally create a second PR for an existing pull request.
    if: >
      !github.event.issue.pull_request &&
      contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association) &&
      github.event.comment.body == '/openhands-fix'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      # ── TIER 1: Claude Code ────────────────────────────────────────────────
      - name: Tier 1 - Claude Code (Claude Pro via OAuth)
        id: primary
        continue-on-error: true
        uses: anthropics/claude-code-action@9d7150bc8a3dae8149739a88019d192b579ad90c # v1.0.193
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
        with:
          github_token: ${{ secrets.PAT_TOKEN }}
          prompt: >
            Please resolve issue #${{ github.event.issue.number }}: "${{ github.event.issue.title }}".
            Issue body: "${{ github.event.issue.body }}".
            1. Write the code to fix the issue completely.
            2. Run tests to verify the fix works.
            3. Commit your changes with a descriptive message.
            4. Create a PR: gh pr create --title "Fixes #${{ github.event.issue.number }}: ${{ github.event.issue.title }}" --body "Fixes #${{ github.event.issue.number }}"
            CRITICAL: If GitHub rejects the merge because checks are still running, poll
            'gh pr checks <number> --watch' and retry. DO NOT FINISH until the PR is created.

      # claude-code-action can exit 0 even on auth/quota errors. Verify the
      # observable outcome instead of trusting the action exit status.
      - name: Check if Claude created a PR
        id: check_claude
        if: github.event.issue.number != ''
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE: ${{ github.event.issue.number }}
          REPOSITORY: ${{ github.repository }}
        run: |
          if [ -z "$ISSUE" ] || [ "$ISSUE" = "null" ]; then
            echo "pr_exists=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          COUNT=$(gh pr list --repo "$REPOSITORY" \
            --search "Fixes #${ISSUE} in:body" \
            --state open --json number -q 'length' 2>/dev/null || echo "0")
          echo "pr_count=$COUNT"
          if [ "$COUNT" -gt 0 ]; then
            echo "pr_exists=true" >> "$GITHUB_OUTPUT"
            echo "Claude created a PR — skipping lower tiers."
          else
            echo "pr_exists=false" >> "$GITHUB_OUTPUT"
            echo "Claude did NOT create a PR — falling through to Tier 2."
          fi

      # ── TIER 2: DeepSeek V3 Chat (paid, heavy lifting) ────────────────────
      - name: Tier 2 - OpenHands (DeepSeek V3 — paid heavy fallback)
        id: secondary
        if: steps.primary.outcome == 'failure' || steps.check_claude.outputs.pr_exists == 'false'
        continue-on-error: true
        uses: xinbenlv/openhands-action@fd1a7b376a5a86c29e3d1a331c75d0ea99b06e04 # v1.0.1
        with:
          github_token: ${{ secrets.PAT_TOKEN }}
          llm_model: "deepseek/deepseek-v4-pro"
          llm_api_key: ${{ secrets.DEEPSEEK_API_KEY }}
          prompt: >
            Please resolve issue #${{ github.event.issue.number }}: "${{ github.event.issue.title }}".
            Issue body: "${{ github.event.issue.body }}".
            1. Write the code to fix the issue completely.
            2. Run tests to verify the fix works.
            3. Commit your changes with a descriptive message.
            4. Create a PR: gh pr create --title "Fixes #${{ github.event.issue.number }}: ${{ github.event.issue.title }}" --body "Fixes #${{ github.event.issue.number }}"
            CRITICAL: DO NOT FINISH until the PR is created and visible on GitHub.
          cli_args: "--max-iterations 50"
          openhands_image: "ghcr.io/all-hands-ai/openhands:0.32"
          runtime_image: "ghcr.io/all-hands-ai/runtime:0.32-nikolaik"

      # OpenHands actions can also report success without the required PR. Check
      # Tier 2's observable result before deciding whether Tier 3 is necessary.
      - name: Check if DeepSeek created a PR
        id: check_secondary
        if: always() && steps.secondary.outcome != 'skipped'
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE: ${{ github.event.issue.number }}
          REPOSITORY: ${{ github.repository }}
        run: |
          COUNT=$(gh pr list --repo "$REPOSITORY" \
            --search "Fixes #${ISSUE} in:body" \
            --state open --json number -q 'length' 2>/dev/null || echo "0")
          echo "pr_count=$COUNT"
          if [ "$COUNT" -gt 0 ]; then
            echo "pr_exists=true" >> "$GITHUB_OUTPUT"
            echo "DeepSeek created a PR — skipping Tier 3."
          else
            echo "pr_exists=false" >> "$GITHUB_OUTPUT"
            echo "DeepSeek did NOT create a PR — falling through to Tier 3."
          fi

      # ── TIER 3: Gemini 3.6 Flash (lightweight last resort) ────────────────
      - name: Tier 3 - OpenHands (Gemini 3.6 Flash — last resort)
        id: tertiary
        if: steps.secondary.outcome == 'failure' || steps.check_secondary.outputs.pr_exists == 'false'
        continue-on-error: true
        uses: xinbenlv/openhands-action@fd1a7b376a5a86c29e3d1a331c75d0ea99b06e04 # v1.0.1
        with:
          github_token: ${{ secrets.PAT_TOKEN }}
          llm_model: "gemini/gemini-3.5-flash"
          llm_api_key: ${{ secrets.GEMINI_API_KEY }}
          prompt: >
            Please resolve issue #${{ github.event.issue.number }}: "${{ github.event.issue.title }}".
            Issue body: "${{ github.event.issue.body }}".
            1. Write the code to fix the issue completely.
            2. Commit your changes with a descriptive message.
            3. Create a PR: gh pr create --title "Fixes #${{ github.event.issue.number }}: ${{ github.event.issue.title }}" --body "Fixes #${{ github.event.issue.number }}"
            CRITICAL: DO NOT FINISH until the PR is created.
          cli_args: "--max-iterations 30"
          openhands_image: "ghcr.io/all-hands-ai/openhands:0.32"
          runtime_image: "ghcr.io/all-hands-ai/runtime:0.32-nikolaik"

      - name: Verify resolver produced a PR or release the dispatch lock
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE: ${{ github.event.issue.number }}
          REPOSITORY: ${{ github.repository }}
        run: |
          COUNT=$(gh pr list --repo "$REPOSITORY" \
            --search "Fixes #${ISSUE} in:body" \
            --state open --json number -q 'length' 2>/dev/null || echo "0")

          if [ "$COUNT" -gt 0 ]; then
            echo "Resolver outcome verified: $COUNT open PR(s) reference issue #$ISSUE."
            exit 0
          fi

          echo "No PR was created by any resolver tier. Releasing swarm-active so the issue can be retried."
          gh issue edit "$ISSUE" --repo "$REPOSITORY" --remove-label "swarm-active" || true
          exit 1
