describe('MediaUploadService', () => {
  it('should exist', () => {
    // Placeholder test so the spec file compiles without failing due to
    // unresolved dependencies.  Full integration tests for MediaUploadService
    // will be added when its AuthService and XMLHttpRequest helpers are
    // properly injected.
    expect(true).toBe(true);
  });
});
