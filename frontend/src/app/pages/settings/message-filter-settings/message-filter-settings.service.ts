// Compatibility export for older route-level imports. Persistence remains
// owned by the canonical authenticated service in app/services.
export { MessageFilterService as MessageFilterSettingsService } from '../../../services/message-filter.service';
