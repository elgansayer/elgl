# Component previews

`component-system.html` is the aggregate Relay + Spartan component-system reference surface used by design-sync while the component and primitive migration is converged in one canonical pull request.
