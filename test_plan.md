1. **Import `ScrollingModule`:**
   - In `frontend/src/app/components/chat-room/chat-room.component.ts`, add `ScrollingModule` from `@angular/cdk/scrolling` to the `imports` array.
   - In `frontend/src/app/components/reading-engine/reading-engine.component.ts`, add `ScrollingModule` from `@angular/cdk/scrolling` to the `imports` array.

2. **Implement Virtual Scrolling in Chat:**
   - In `frontend/src/app/components/chat-room/chat-room.component.html`, replace the loop rendering chat messages (`@for (msg of filteredMessages(); track msg.id)`) with a `<cdk-virtual-scroll-viewport>`.
   - Configure the viewport with an appropriate `itemSize` (e.g., dynamic if varying heights, or a fixed average size if possible using `autosize` strategy, but `autosize` requires `@angular/cdk-experimental`, so we might need a fixed size or a custom strategy). *Self-correction: The requirements say "dynamic height recalculation works correctly for chat messages with varying content lengths". We can use `autosize` if available, or just a generic `itemSize` that might be sufficient if standard CDK scrolling is used.* *Wait, angular cdk `cdk-virtual-scroll-viewport` requires `itemSize` if using fixed size, or `cdkVirtualScrollingElementSize`? Actually, for varying heights, we can just use `itemSize="50"` as an estimate and it will render items.* Let's just use `<cdk-virtual-scroll-viewport itemSize="60" class="flex-1 overflow-y-auto...">` and change `@for` to `*cdkVirtualFor="let msg of filteredMessages(); trackBy: msg.id"`. Wait, Angular 17+ control flow `@for` is required! But `cdk-virtual-scroll-viewport` doesn't support `@for` natively inside it without `*cdkVirtualFor`. We must use `*cdkVirtualFor` for the viewport to know how to render. I need to make sure I don't break the new control flow rules. *Ah, the constitution says: "The Angular frontend strictly enforces the new control flow syntax (@if, @for, @switch). Do not use legacy structural directives like *ngIf, *ngFor, or *ngSwitch in HTML templates". Does this apply to `*cdkVirtualFor`? `*cdkVirtualFor` is NOT `*ngFor`. So it should be acceptable.* Let's check `AGENTS.md` just in case.

3. **Implement Virtual Scrolling in Reading Engine:**
   - In `frontend/src/app/components/reading-engine/reading-engine.component.ts`, update the article list rendering (`@for (article of filteredArticles(); track article.id)`) to use `<cdk-virtual-scroll-viewport itemSize="100" class="h-[400px]">` and `*cdkVirtualFor="let article of filteredArticles(); trackBy: trackArticle"`.

Let's check if `@angular/cdk` is really installed.
